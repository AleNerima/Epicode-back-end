@model IEnumerable<ScarpeCo.Models.Product>

@{
    ViewData["Title"] = "Product List";
}

<h1 class="my-4">Product List</h1>

<div class="row">
    @foreach (var product in Model)
    {
        <div class="col-md-3 mb-4">
            <div class="card">
                <img class="card-img-top" src="data:image/png;base64,@product.CoverImageBase64" alt="@product.Name" />
                <div class="card-body">
                    <h5 class="card-title">@product.Name</h5>
                    <p class="card-text">Price: @product.Price.ToString("C")</p>
                    <a href="@Url.Action("Details", "Product", new { id = product.Id })" class="btn btn-primary">View Details</a>
                </div>
            </div>
        </div>
    }
</div>
